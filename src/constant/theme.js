import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0E185F",
    },
    secondary: {
      main: "#FF8AAE",
    },
  },
});

export default theme;
